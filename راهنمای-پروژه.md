# راهنمای پروژه ai-context-packer (نسخه‌ی خودکار با GitHub Actions)

## این زیپ چیه؟
یک پروژه‌ی کامل و آماده که نیازی به هیچ تنظیم دستی اندروید ندارید. پوشه‌ی `android/`
عمداً داخلش نیست — هر بار که `push` می‌کنید، خودِ GitHub Actions اون رو از صفر می‌سازه،
پلاگین حباب شناور رو داخلش کپی می‌کنه، Manifest رو پچ می‌کنه، و APK می‌سازه. یعنی شما فقط
کافیه فایل‌های `www/index.html` یا `android-plugin/*.kt` رو ویرایش کنید، دوباره zip کنید،
و push کنید.

## محتویات پروژه
```
ai-context-packer/
├── package.json                 ← وابستگی‌های Capacitor
├── capacitor.config.json        ← appId ثابت: com.aiyerbeyer.aicontext
├── .gitignore                   ← node_modules و android/ رو نادیده می‌گیره
├── www/index.html                ← خود اپ (سه تب: تکی، گروهی، حباب شناور)
├── android-plugin/
│   ├── OverlayPlugin.kt
│   ├── OverlayService.kt
│   └── MainActivity.kt
└── .github/workflows/android-build.yml   ← کل فرآیند build خودکار
```

## اولین بار: ساخت ریپازیتوری
یک ریپازیتوری خالی به اسم `ai-context-packer` در گیت‌هاب بسازید (بدون README).

## دستورات ترموکس
```bash
cd ~
rm -rf ai-context-packer
unzip -o ~/storage/downloads/ai-context-packer.zip -d ~/
cd ~/ai-context-packer

git init
git add .
git commit -m "initial: overlay bubble capture mode"
git branch -M main
git remote add origin https://github.com/aiyerbeyer/ai-context-packer.git
git push -u origin main --force
```
همون‌جا که یوزرنیم/توکن گیت‌هاب رو می‌زنید، کافیه.

## دفعات بعدی (وقتی چیزی رو تغییر دادید)
دقیقاً همون کاری که با market-monitor انجام می‌دید: فایل‌های تغییریافته رو (مثلاً
`www/index.html` جدید) داخل همین ساختار پوشه جایگزین کنید، دوباره zip بگیرید، و همین
دستورات بالا رو (که `rm -rf` هم داخلشه) دوباره اجرا کنید. چون `android/` اصلاً commit
نشده، هیچ تداخل یا نیاز به پاک‌سازی خاصی نیست.

## گرفتن APK
بعد از push، تب **Actions** توی ریپازیتوری رو باز کنید؛ اجرای «Build Android APK» حدود
۳ تا ۶ دقیقه طول می‌کشه (چون این‌بار هم باید Android SDK/Gradle دانلود بشه هم پروژه
اندروید از صفر ساخته بشه — کمی کندتر از قبل، ولی کاملاً خودکار). وقتی تیک سبز خورد،
پایین صفحه‌ی همون اجرا، بخش **Artifacts** یک فایل `app-debug-apk` هست؛ دانلودش کنید
(یک zip حاوی `app-debug.apk` می‌ده).

## اگر Action قرمز شد (fail)
رایج‌ترین دلیل: نسخه‌ی جدید Capacitor ساختار AndroidManifest.xml رو کمی عوض کرده و
دستورات `sed` داخل workflow دیگه با متن فایل match نمی‌کنن. توی لاگ Action، مرحله‌ی
«افزودن مجوزها و سرویس» رو باز کنید — اگه دیدید `cat` نشون داد که `<service>` یا
permission‌های جدید اضافه نشدن، این خط‌های sed نیاز به به‌روزرسانی دارن. کافیه محتوای
فعلی `AndroidManifest.xml` (از همون لاگ) رو برام بفرستید تا سد جدید رو تنظیم کنم.

## جریان کار داخل اپ (یادآوری)
1. تب «حباب شناور» → «شروع ضبط» (بار اول مجوز overlay رو می‌ده).
2. نوار شناور دو دکمه داره: **👤 سوال** و **🤖 پاسخ**. متن رو هرجا کپی کن و روی دکمه‌ی
   مقصدش بزن — append و تشخیص تکراری برای هر مقصد جداگانه انجام می‌شه.
3. مفهوم «فاز» حذف شده. وقتی پاسخ ثبت شده و دوباره 👤 بزنی، ترن قبلی خودکار ذخیره
   می‌شه و سوال بعدی شروع می‌شه — بدون سوییچ دستی و بدون باز کردن اپ.
4. دکمه‌ی **⋯** روی نوار، ردیف «لغو / پایان» رو باز می‌کنه. «پایان» از داخل نوتیفیکیشن
   هم در دسترسه.
5. هر وقت تموم شد → «پایان» → خروجی XML/MD/JSON پایین صفحه آماده‌ست.

نکته‌ی فنی: پیام‌های تأیید با `showToast` نیتیو نشون داده می‌شن، چون toastهای داخل WebView
وقتی اپ در پس‌زمینه‌ست دیده نمی‌شن. رویدادها هم با `retainUntilConsumed=true` فرستاده می‌شن
تا اگر WebView لحظه‌ای معلق شده باشه، گم نشن.
